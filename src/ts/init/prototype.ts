import find from "../externals/prototypes/find";
import findIndex from "../externals/prototypes/findIndex";

export default class prototypes {

    constructor() {
        const f = new find();
        const fi = new findIndex();
    }
}
